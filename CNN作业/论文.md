# readme
# 论文名字：图像理解中的卷积神经网络
这篇论文的研究方向是向读者介绍CNN的基础理论，介绍CNN过程中的卷积池化正则和损失函数。
网络结构围绕着卷积层、池化层和全连接层展开，介绍了每个层的作用，同时还举了早期经典的
LeNet-5网络结构（2卷积+2池化+2全连接）的例子。训练方式由两个部分组成，先是由随机的
权重得到预测输出，然后再根据真实输出与预测输出的差值，反向回推，对每个层求导，更新每一个
层级的权重数。数据集论文推荐了ImageNet（ILSVRC），LFW（Labeled Faces in the Wild）
，这两个分别是标注图像数据集和人脸数据集。